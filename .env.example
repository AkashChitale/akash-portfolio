# Replace before a public deployment. Never place secrets in PUBLIC_ variables.
SITE_URL=https://akashchitale.dev
# Optional public, read-only HTTPS endpoint. Leave empty to disable all API requests.
PUBLIC_SYSTEMS_LAB_URL=
